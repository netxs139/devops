# DevOps 平台建设进度跟踪 (Progress Tracker)

## 🎯 当前重点 (Current Focus)
- **核心重点**: v3.0 阶段三：前端 BI 大屏重构 (DORA & Gitprime)
- **状态**: 🟢 已完成

## 🛠️ 任务清单 (Tasks)

**【v3.0 模块化架构演进 (Modular Monolith Evolution)】**

1. [阶段一：敏捷模块深度解耦] 将需求、缺陷、迭代看板核心业务从 `devops_collector` 彻底剥离至独立的 `agile_module` 包，并将跨域硬外键全部重构为 `String(32)` 软引用。 🟢 已完成
2. [阶段二：测试模块物理拆分] 新建 `test_module` 包，承接用例管理与执行计划，并完成与敏捷缺陷投递的单向解耦。 🟢 已完成
3. [阶段三：BI 度量中心建设] 新建只读性质的 `bi_metrics_module`，基于 dbt 数据仓封装 DORA 指标与 ELOC 代码贡献聚合 API。 🟢 已完成
4. [阶段四：主数据网关收敛] 清理 `devops_collector` 遗留代码，纯化为专职对接外部系统的 `mdm_core_module` 事件广播中心。 ⏳ 待实现
5. [阶段五：微前端基座拼装] 在前端 Shell 底座中彻底抽离遗留的强耦合页面，完成 `@company/agile-kit` 等远程业务联邦包的动态挂载集成。 ⏳ 待实现
6. [阶段六：端到端质量防线] 针对拼装成型的五大模块网关，落地 Playwright E2E 测试框架，贯穿登录、核心数据同步与提单的用户旅程。 ⏳ 待实现

## 📋 最近完成 (Recently Completed)

1. [2026-07-06] **v3.0 阶段四：BI 前端大屏重构闭环**: 彻底下线 Streamlit 渲染引擎，构建了 `DoraMetricsView.vue` 和 `GitprimeView.vue`，实现数据和视图的双向解耦。落地了 Visibility 感知暂停、ECharts 生命周期释放及前端性能优化。实施了构建“三角优化”（Vite剥离tsc，tsc增量编译，vite-plugin-checker），修复了Docker跨库依赖卷挂载死锁及Worker冗余。全量通过 `just verify` 验证。

1. [2026-07-06] **v3.0 阶段三：BI 度量中心建设 (后端层)**: 完成了 BI 数据壳的后端架构搭建。创建了直接映射 `dbt_project/models/marts/` 的只读 `marts.py` SQLAlchemy 模型，封装了对应的 Pydantic schemas。在 `bi_metrics_router.py` 内实现了带 TTL 缓存屏障的 FastAPI 路由，以防御前端高频轮询引发的数据库雪崩。移除了遗留的 Streamlit 依赖与相关容器配置，打通了集成测试验证。

1. [2026-07-06] **[Ad-hoc] Iteration → Sprint 全栈重命名重构**: 全面完成后端 API、前端遗留 JS 组件、Vue 3 接口以及相关文档和数据大盘 SQL 的更名工作，解决 CI detect-secrets 误报，代码落库成功。commit: `967833b`。

1. [2026-07-06] **v3.0 阶段二：测试模块物理拆分**: 建立 `test_module` 独立包，将用例管理与执行计划从 `devops_collector` 彻底解耦。重构并删除了旧版硬关联模型与路由，通过 SQLAlchemy 软引用 (`viewonly=True`) 机制修复了 `admin_service` 跨域查询导致的大面积 mypy 报错。修复遗留的 Vue SFC 类型缺陷，全量 `just verify` 验证通过。commit: `6426539`。
2. [2026-07-06] **v3.0 阶段一：敏捷模块深度解耦**: 将 EpicMaster 模型从 `devops_collector` 剥离至 `agile_module/models`，并将 `owner_id`/`org_id`/`okr_objective_id` 等跨库外键全部转换为软引用 (`UUID`/`Integer`)；将 `iteration_plan_service` 和 `iteration_core_service` 迁移至敏捷模块专属服务层；将网关路由 `iteration_plan_router` 挂载路径更名为 `/agile/iteration`。导入验证通过。commit: `5800ec0`。

---

*Generated by Antigravity-AI (2026-06-20T12:59:00+08:00)*
