# 项目物理目录地图 (PROJECT_MAP.md)

> **定位声明**：本项目物理目录结构的“索引图”，明确定义各模块的业务边界与 AI 协作范围，防止跨域污染。

______________________________________________________________________

## 🏗️ 1. 核心代码域 (Core Codebase)

| 目录路径 | 核心业务定义 | AI 协作红线 |
| :--- | :--- | :--- |
| `devops_collector/` | **数据采集内核** (Python/FastAPI) | 严禁在此存放 UI 逻辑；核心模型变更需触发 `mdm-integrity-arbiter`。 |
| `devops_portal/` | **前端门户** (Python/Streamlit) | 仅限展示与交互逻辑；严禁直接操作核心生产数据库。 |
| `dbt_project/` | **数据建模与转换** (SQL/dbt) | 遵循 MDS 分层规范；必须通过 `dbt-pipeline-auditor` 审计。 |

## 🛠️ 2. 工具与基础设施 (Tools & Infrastructure)

| 目录路径 | 核心业务定义 | AI 协作红线 |
| :--- | :--- | :--- |
| `scripts/` | **维护工具箱** | 仅存放一次性初始化、环境诊断脚本；严禁存放业务核心逻辑。 |
| `.agent/` | **AI 协作基座** | 存放 SOP 流程 (`workflows/`)、专家技能 (`skills/`) 及隔离沙盒 (`scratch/`)。 |
| `.github/` | **CI/CD 工作流** | 定义 GitHub Actions 自动化流水线。 |

## 📖 3. 知识与资产 (Knowledge & Assets)

| 目录路径 | 核心业务定义 | AI 协作红线 |
| :--- | :--- | :--- |
| `docs/` | **项目知识库 (SSOT)** | 所有设计、API 字典及历史教训的唯一来源。 |
| `tests/` | **全链路测试套件** | 包含 unit, integration, e2e 各层级验证代码。 |
| `data/` | **本地持久化数据** | 仅限本地测试或临时数据缓存。 |

## 📂 4. 临时与归档 (Temporary & Archived)

| 目录路径 | 核心业务定义 | AI 协作红线 |
| :--- | :--- | :--- |
| `scratch/` | **公共草稿纸** | 人类开发者的临时代码区；AI 建议避开此处。 |
| `archived/` | **历史遗迹** | 存放已废弃但保留审计价值的陈旧代码/文档。 |

______________________________________________________________________

*Generated by Antigravity-Core (2026-04-30)*
